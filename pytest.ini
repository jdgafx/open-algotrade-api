[pytest]
# Integration tests hit a live API server (localhost:8002) and are deselected by
# default. Run them explicitly with: pytest -m integration  (server must be up).
markers =
    integration: requires a running API server (deselected by default)
addopts = -m "not integration"
