# ── Core Framework ──
fastapi
uvicorn[standard]
pydantic
python-dotenv
python-multipart

# ── Database ──
sqlalchemy

# ── Auth ──
python-jose[cryptography]
passlib[bcrypt]
bcrypt

# ── Hyperliquid SDK & Web3 ──
hyperliquid-python-sdk
eth-account
web3

# ── Data / Analytics ──
pandas
numpy
scipy  # required by src/engine/rigor/validation.py (Deflated Sharpe / CPCV) — was present in the venv but unpinned; the live promotion gate now imports it
requests
httpx

# ── Strategies & Backtesting ──
hmmlearn
backtesting
ccxt

# ── Optimization & Scheduling ──
optuna>=3.6.0
apscheduler>=3.10.0
anthropic>=0.25.0

# ── Billing ──
stripe

# ── WebSocket support ──
websockets

# ── Solana integration (Jupiter/Birdeye API calls via httpx, already above) ──
# solana and solders not needed yet - using REST APIs via httpx
